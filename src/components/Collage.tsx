import React from 'react';

// This component is intentionally left empty/placeholder as the specific "Collage"
// wasn't in the provided text flow, but we keep the file to avoid breaking imports in App.tsx
// until strictly removed there. It effectively renders nothing now to adhere to the requested layout.

export const Collage: React.FC = () => {
  return null;
};
